name: API Key Validation
on:
  schedule:
    - cron: '30 2 * * *'  

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - name: Check keys
      run: |
        python scripts/validate_keys.py
